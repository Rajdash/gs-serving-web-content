---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: openjdk
    tag: '8-jdk-alpine'

inputs:
  - name: source-code

outputs:
  - name: build-output

run:
  path: 
   chmod 755 /source-code/ci/build.sh
  ./source-code/ci/build.sh
